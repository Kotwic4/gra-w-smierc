package GameManager;

import javafx.scene.paint.Color;

public class Player {
    Color color;
    int id;

    public Player(Color color, int id) {
        this.color = color;
        this.id = id;
    }
}
